let tweet = 'Woohoo! :-)';

if (tweet.length() > 140) {
  console.log('Tweet is too long!');
}
/* 
Check the documentation of both TypeError and length, in order to find out what 
causes the error.
*/

// .length does not need (). Length is a property. 